inherit_from: ../.rubocop.yml

plugins:
  - rubocop-rspec
  - rubocop-rspec_rails

# RSpec specific configuration
RSpec:
  Language:
    Includes:
      Examples:
        - it_behaves_like
        - it_should_behave_like
